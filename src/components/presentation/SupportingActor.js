import React from 'react'

const SupportingActor = (props)=>{
  return(
    <div>
        <span>{props.currentNomination.supportingActorName}</span><br /><br />
    </div>
  )
}

export default SupportingActor
